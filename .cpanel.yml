---
deployment:
  tasks:
    # Copy public-facing files into public_html
    - export DEPLOYPATH=$HOME/public_html
    - /bin/cp -r public/. $DEPLOYPATH

    # src/ and database/ must live at $HOME (one level above public_html)
    # because index.php uses dirname(__DIR__) which resolves to $HOME
    - /bin/cp -r src $HOME/src
    - /bin/cp -r database $HOME/database

    # Copy .env to $HOME if it doesn't already exist
    - "[ -f $HOME/.env ] || /bin/cp .env.example $HOME/.env"

    # Ensure the images upload directory exists and is writable
    - /bin/mkdir -p $DEPLOYPATH/images
    - /bin/chmod 755 $DEPLOYPATH/images

    # Fix permissions on PHP files
    - /bin/find $DEPLOYPATH -name "*.php" -exec /bin/chmod 644 {} \;
